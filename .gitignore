# OS / editor cruft
.DS_Store
*~
~$*

# Node (none committed, but just in case)
node_modules/

# Local-only API base layer is regenerated by the fetcher; safe to ignore if you like.
# (Leave it tracked if you want the GitHub Action to diff against it — see deploy guide.)
